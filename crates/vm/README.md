# shadowjs-vm

The Virtual Machine for ShadowJS.

This crate implements the stack-based virtual machine that executes the bytecode generated by `shadowjs-bytecode`. It manages the call stack, scope chain, and instruction dispatch loop.
