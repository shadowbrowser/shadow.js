# shadowjs-value

Value representation for ShadowJS.

This crate defines the `Value` enum, which represents all possible JavaScript values in the engine (e.g., `Number`, `String`, `Boolean`, `Object`, `Array`, `Null`, `Undefined`).
