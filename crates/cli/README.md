# shadowjs-cli

The Command Line Interface for ShadowJS.

This crate provides the `shadowjs` executable, allowing users to run JavaScript files directly from the terminal.

## Usage

```bash
cargo run -p shadowjs -- <file.js> [options]
```

### Options

*   `--bench`: Measure and print execution time.
*   `--debug`: Print the generated bytecode before execution.
