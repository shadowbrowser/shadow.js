# shadowjs-parser

The Parser for ShadowJS.

This crate takes the stream of tokens produced by `shadowjs-lexer` and constructs an Abstract Syntax Tree (AST) defined in `shadowjs-ast`. It implements a recursive descent parser.
